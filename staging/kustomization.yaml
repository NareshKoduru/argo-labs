apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../base
namespace: staging
commonAnnotations:
  supported-by: sre@example.com
  test: koduru
labels:
- includeSelectors: false
  pairs:
    project: instavote
patches:
- path: service.yaml
